# Cursor Log — 2026-06-27

## [Cursor Log] Mobile Web PWA (phone field runner)

- failed: no mobile/web lane existed — desktop-only (PySide6 + Qt WebEngine + USB GPS + PicoCount). First stress run also flagged `rapid_map_state` p95=181ms over a 150ms budget, and an optional 80-stop street-trace took 227s on this box (full CA road graph present).
- changed: added a standalone online-first lane that reuses desktop core unchanged. New: `core/map_state.py` (Qt-free map payload); `mobile_web/` (`server.py` FastAPI, `store.py` token-scoped JSON job store with in-memory cache, `static/` lean 4-tab PWA — Route/Install/Pickup/Audit — with phone-geolocation grab + drop-pin fallback, service worker, manifest, vendored MapLibre + online OSM tiles); `scripts/run_mobile.py` + `RUN_MOBILE.bat` (LAN launcher); proof scripts `scripts/mobile_user_prove.py`, `scripts/mobile_stress_loop.py`, `scripts/mobile_app_check.py`; `mobile_web/requirements.txt`; `docs/MOBILE_WEB.md`. Improvements from the stress findings: added the store in-memory cache (O(1) loads), and reworked the large-job scenario to measure the payload/edit latency the phone actually hits instead of the heavy server-side street-trace.
- rationale: Director wants a lean, fast, smooth phone version on phone internet — not a desktop clone. USB GPS and PicoCount are laptop-only by design; the phone captures exact location from the browser at install. Desktop app left untouched (new files only; `core/map_state.py` is additive and unused by the desktop path).
- result: `python scripts/mobile_app_check.py` PASS — user flow 20/20 (import, route, phone GPS grab, pin fallback, install, pickup, export, error paths; token enforced; out-of-bounds GPS + bad upload rejected); stress PASS (install cycle clean over 5 stops; map-state p50=110ms/p95=166ms under 250ms budget; concurrent grab+patch no lost writes; 80-stop map-state 36.7KB). Audit: `logs/mobile_check/latest.md`. Lint clean on all new files.
- regression: `python scripts/smoke_full.py` — functional checks PASS (core imports, road graph 11544 nodes, field readiness 100/100, offline gates, golden demo route in band). 2 FAILs are route-build **time budgets only** on a slow/loaded box (`benchmark_route` >300s; golden `n20`/`n40` 147s/241s vs 60s/86s), with correct mileage (2.6 mi, in band), in routing code this change never touches — pre-existing environmental, not a mobile-lane regression. Escalating the perf budgets to Director rather than tuning unrelated routing.
- push: committed to feature branch `feature/mobile-web-pwa`; **push held** (base is `main`, and desktop smoke not 100% green due to unrelated perf budgets) — Director to review/promote.
