## [Cursor Log] Field-day hardening + battery saver (v1.0.8)

**Summary:** Audited stability, added AC/battery-aware throttling, fixed GPS timer start, hardened shutdown — smoke + stress PASS.

**Changes:**
- File: `core/power.py` — Windows `GetSystemPowerStatus` AC/battery detection
- File: `ui/simple_mode.py` — `timing_profile()` with battery vs plugged intervals
- File: `main.py` — power poll (30s), status bar pill, apply profile to GPS/map/save timers; `closeEvent` stops all worker threads; GPS timer `.start()` fix
- File: `version.py` — 1.0.8
- File: `scripts/smoke_full.py` — power/battery/close/gps-timer checks
- File: `HOW_TO_RUN.md` — battery saver docs

**Proof:**
- `scripts/smoke_full.py` exit 0 (all green, v1.0.8)
- `scripts/stress_loop.py --full --cycles 1` exit 0 (picocount sandbox WARN — no hardware)

**Notes:** Plugged = 450ms GPS follow tick; battery = 900ms + fewer map pushes. Unknown AC (desktop) uses full rate.

## [Cursor Log] Hardware audit + lean-drive perf (v1.0.8)

**Summary:** Re-ran smoke/stress with GPS fix + PicoCount on COM10; wired lean_drive map path; fixed port probing and non-blocking GPS diagnose.

**Changes:**
- File: `web/app.js` — `applyLeanDriveData`: skip segment/stop rebuild, hide basemap detail layers, no route casing, skip compass updates while driving
- File: `core/picocount.py` — `is_gps_port` / `is_counter_port`; probe skips GPS COM ports; stop probing wrong port on FTDI busy
- File: `gps_reader.py` — `diagnose()` uses GPSStream (no blocking `get_fix` port scan)
- File: `scripts/hardware_check.py` — new morning script for GPS + counter
- File: `scripts/smoke_full.py` — lean drive + port helper checks

**Proof:**
- `scripts/smoke_full.py` exit 0
- `scripts/hardware_check.py` — GPS FIX COM9; PicoCount COM10 PC2500 V2.41 serial 24022816
- `scripts/picocount_sandbox.py --port COM10` exit 0
- `scripts/stress_loop.py --full` exit 0 (all green incl. picocount_sandbox)

**Notes:** COM10 denied when GPS stream holds bus — app already pauses GPS before counter ops; run hardware_check with app closed.
