Demo field files — desk test without real job data
==================================================

Files:
  demo_sites.csv   — 5 sites near Orange County (begin/end lat/lon + street)
  DemoDay.EST      — map file listing site IDs 101–105

In the app:
  Setup → "Load demo files" → Download road map → BUILD OPTIMIZED ROUTE

Or add these paths manually via Add Excel/CSV and Add .EST.
