[Cursor Log] 2026-06-25 — P46 main.py slim v2 (shell extract)

**baseline:** `smoke_full.py` exit 0 before edits; `main.py` ~587 lines.

**inventory (main.py before):**
- WebEngine env + `MainWindow.__init__` wiring (state, timers, map view, GPS)
- Field mode helpers (`_internet_allowed`, `_sync_field_mode`)
- Power/GPS timing (`_poll_power`, `_apply_power_profile`, timer sync)
- GPS bridge throttle (`_should_push_gps_bridge`)
- Shell layout (`_build_ui`, nav rail, placeholder, scroll wrap)
- Dialogs + `closeEvent`
- `main()` startup (crash log, auto-update, show/maximize)

**changed:**
- `ui/shell/field_mode.py` — `FieldModeMixin`
- `ui/shell/power_profile.py` — `PowerProfileMixin`
- `ui/shell/main_layout.py` — `ShellLayoutMixin` (build UI, field nav shell, placeholder)
- `ui/shell/lifecycle.py` — `ShellLifecycleMixin` (`closeEvent`, `_info`/`_warn`)
- `ui/controllers/map_sync.py` — `_should_push_gps_bridge` (next to `_tick_gps`)
- `main.py` — wiring-only shell (~256 lines); mixin MRO updated
- `ui/shell/__init__.py` — exports

**proof (after each batch + final):**
- `python -c "from main import MainWindow"` OK
- `smoke_full.py` exit 0 (all checks green)

**result:** P46 line budget met (256 ≪ 1200). Behavior preserved per smoke; **not** field-ready / HANDOFF-PROOF.

**git:** hold push on `main`.

---

[Cursor Log] 2026-06-25 — P46 main.py slim v2 (startup extract)

**failed:** First post-extract smoke failed only in `scripts/test_ui_wiring.py`; the bridge wiring had moved from `main.py` to `ui/shell/startup.py`, but one static check still scanned `main.py` directly.

**changed:**
- `ui/shell/startup.py` — new `ShellStartupMixin` with named startup phases: state/flags, persistence timers, map view/bridge, shell UI refresh, GPS/deferred refresh.
- `main.py` — `MainWindow.__init__` now sets the title and delegates startup to `_init_window_startup()`; `main.py` is 106 lines.
- `scripts/test_ui_wiring.py` — bridge wiring check now uses `_shell_src()` like the rest of the slimmed shell audit.

**rationale:** This keeps startup order unchanged while moving stateful setup out of the entry file. The static audit update preserves coverage across extracted shell modules.

**proof:**
- `python -c "from main import MainWindow, main"` OK
- `scripts/test_ui_wiring.py` exit 0
- `scripts/smoke_full.py` exit 0 (all checks green)

**result:** P46 consolidation advanced; behavior preserved per smoke. No field-ready / HANDOFF-PROOF claim.

---

[Cursor Log] 2026-06-25 — P46 pure-helper + app-entry extract

**baseline:** `smoke_full.py` exit 0 before edits; `main.py` 106 lines (P46 line budget already met).

**inventory (main.py responsibilities):**
- WebEngine env bootstrap (`apply_webengine_env`) before Qt imports
- `MainWindow` mixin MRO + title + `_init_window_startup()` delegate
- `main()` → `ui.app_entry.launch_app()`

**changed (batch 1 — pure/stateless):**
- `ui/map_helpers.py` — `dist_m`, `coords_moved`, `heading_cardinal`, `should_push_gps_bridge`, `display_route_for_map`, `shift_has_field_progress`
- `ui/controllers/map_sync.py` — thin delegates to `map_helpers`; removed inline math

**changed (batch 2 — entry split):**
- `ui/app_entry.py` — `launch_app()` (crash log, portable auto-update, `QApplication`, show/maximize)
- `main.py` — 57 lines; shell + env only

**audit:**
- `scripts/smoke_full.py` — `map_helpers` unit checks in `[imports]`; `_shell_src()` includes `app_entry.py`
- `scripts/test_ui_wiring.py` — `_shell_src()` includes `app_entry.py`

**proof (after each batch):**
- `python -c "from main import MainWindow, main"` OK
- `scripts/test_ui_wiring.py` exit 0
- `scripts/smoke_full.py` exit 0 (all checks green)

**result:** P46 consolidation continued; behavior preserved per smoke. **Not** field-ready / HANDOFF-PROOF.

**git:** hold push on `main`.
