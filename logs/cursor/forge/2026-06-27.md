# Cursor Log — 2026-06-27

## [Cursor Log] Mobile Web PWA (phone field runner)

- failed: no mobile/web lane existed — desktop-only (PySide6 + Qt WebEngine + USB GPS + PicoCount). First stress run also flagged `rapid_map_state` p95=181ms over a 150ms budget, and an optional 80-stop street-trace took 227s on this box (full CA road graph present).
- changed: added a standalone online-first lane that reuses desktop core unchanged. New: `core/map_state.py` (Qt-free map payload); `mobile_web/` (`server.py` FastAPI, `store.py` token-scoped JSON job store with in-memory cache, `static/` lean 4-tab PWA — Route/Install/Pickup/Audit — with phone-geolocation grab + drop-pin fallback, service worker, manifest, vendored MapLibre + online OSM tiles); `scripts/run_mobile.py` + `RUN_MOBILE.bat` (LAN launcher); proof scripts `scripts/mobile_user_prove.py`, `scripts/mobile_stress_loop.py`, `scripts/mobile_app_check.py`; `mobile_web/requirements.txt`; `docs/MOBILE_WEB.md`. Improvements from the stress findings: added the store in-memory cache (O(1) loads), and reworked the large-job scenario to measure the payload/edit latency the phone actually hits instead of the heavy server-side street-trace.
- rationale: Director wants a lean, fast, smooth phone version on phone internet — not a desktop clone. USB GPS and PicoCount are laptop-only by design; the phone captures exact location from the browser at install. Desktop app left untouched (new files only; `core/map_state.py` is additive and unused by the desktop path).
- result: `python scripts/mobile_app_check.py` PASS — user flow 20/20 (import, route, phone GPS grab, pin fallback, install, pickup, export, error paths; token enforced; out-of-bounds GPS + bad upload rejected); stress PASS (install cycle clean over 5 stops; map-state p50=110ms/p95=166ms under 250ms budget; concurrent grab+patch no lost writes; 80-stop map-state 36.7KB). Audit: `logs/mobile_check/latest.md`. Lint clean on all new files.
- regression: `python scripts/smoke_full.py` — functional checks PASS (core imports, road graph 11544 nodes, field readiness 100/100, offline gates, golden demo route in band). 2 FAILs are route-build **time budgets only** on a slow/loaded box (`benchmark_route` >300s; golden `n20`/`n40` 147s/241s vs 60s/86s), with correct mileage (2.6 mi, in band), in routing code this change never touches — pre-existing environmental, not a mobile-lane regression. Escalating the perf budgets to Director rather than tuning unrelated routing.
- push: committed to feature branch `feature/mobile-web-pwa`; **push held** (base is `main`, and desktop smoke not 100% green due to unrelated perf budgets) — Director to review/promote.

## [Cursor Log] Mobile HTTPS — phone GPS now works over LAN

- failed: the mobile lane's #1 field blocker — phone browser geolocation needs a secure context, but the launcher served plain `http://<lan-ip>:8800`, so `getCurrentPosition` was silently blocked in the field; only the drop-pin fallback worked. The whole point of the phone lane (capture exact install GPS) was unusable off `localhost`.
- changed: added `mobile_web/tls.py` (self-signed cert generator; SAN names this PC's LAN IP + `127.0.0.1` + `localhost`; cached in git-ignored `tds_data/mobile_certs/`, regenerated on host/expiry change). Reworked `scripts/run_mobile.py` to default to HTTPS (passes `ssl_certfile`/`ssl_keyfile` to uvicorn), print `https://` URLs + a one-time-warning note, with `TD_MOBILE_HTTP=1` opt-out and graceful HTTP fallback if `cryptography` is missing. Added `cryptography` to `mobile_web/requirements.txt` and the `RUN_MOBILE.bat` dep check. New proof `scripts/mobile_tls_prove.py` (boots real uvicorn HTTPS, pins trust to our cert). Wired `tls_prove` into `scripts/mobile_app_check.py` and refreshed its improvement backlog; updated `docs/MOBILE_WEB.md`. Desktop path untouched (additive; `core`/Qt shell unchanged).
- rationale: a self-contained field tool can't depend on an external HTTPS proxy. A self-signed cert that names the LAN IP gives a real secure context so the phone's "Grab GPS" works after a one-time "proceed" — no internet, no keys in the repo.
- result: `python scripts/mobile_tls_prove.py` PASS 11/11 (cert SAN has LAN IP + loopback + localhost; cert reused when fresh; real TLS handshake serves `/api/healthz`=200 ok; plain HTTP rejected on the TLS port; `TD_MOBILE_HTTP=1` opt-out resolves to no cert). `python scripts/mobile_app_check.py` PASS — user 20/20, TLS 11/11, stress clean (map-state p95 2.6ms<250ms, install cycle clean, concurrent writes safe). Error path: plain-HTTP-on-TLS-port and missing-`cryptography` fallback both exercised. Lint + `py_compile` clean on all four files. Audit: `logs/mobile_check/latest.md`.
- push: feature branch `feature/mobile-web-pwa`; mobile-only/additive. Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.

## [Cursor Log] Mobile public SHARE mode (text a link, phone works anywhere)

- failed: the mobile lane only worked on the same Wi-Fi/hotspot — no way to send a crew member a link that opens a job from cellular. Director wanted the old Streamlit "share a link, phone works anywhere" behavior. Recommendation taken: keep the FastAPI/PWA (reuses desktop `core/`), add share-only public mode + a tunnel, rather than re-adopting Streamlit (removed at v1.0.2 for the offline desktop tool).
- changed: `mobile_web/settings.py` (new) — share-only public mode (`TD_MOBILE_PUBLIC=1`), admin key (`TD_MOBILE_ADMIN_KEY`, auto-generated if unset in public mode), and public base URL (`TD_MOBILE_PUBLIC_URL`). `mobile_web/server.py`: `/join/{id}` share-link entry (serves the PWA, client auto-opens the job + strips the token from the URL), `/api/jobs/{id}/share` (link) and `/api/jobs/{id}/share.svg` (QR via `segno`), `share_url` in create responses, admin-gated `demo`/`import` creation, and `public_mode`/`can_create` in `/api/config`. PWA: `app.js` parses `/join/<id>?token=`, applies public mode (hides creation, shows share note), Audit-tab share link + Copy + QR; `index.html` wraps creation in `#createBlock`, adds share UI, bumps `app.js?v=2`; `style.css` public-mode + share styles. Launcher: `scripts/run_mobile_share.py` + `RUN_MOBILE_SHARE.bat` — public mode + `cloudflared` quick tunnel, discovers the public URL, feeds it back for absolute links, `--demo` prints a ready-to-send share link. Proof `scripts/mobile_share_prove.py`; wired `share_prove` into `mobile_app_check.py` + refreshed backlog; `segno` added to `mobile_web/requirements.txt` and both RUN_MOBILE bats; `docs/MOBILE_WEB.md` updated. Desktop untouched.
- rationale: each job already had a random token, so the access model was there — it just needed a public URL, a `/join` share link, and a way to stop random visitors creating/browsing jobs. Cloudflare quick tunnel gives a public HTTPS URL with no port-forwarding; the link itself is the per-job credential and is removed from the address bar after load. Always-on hosting (Render/Fly/VPS) is deliberately deferred to phase 2.
- result: `python scripts/mobile_share_prove.py` PASS 19/19 — LOCAL: not-public config, demo without key, absolute `share_url` with `/join/<id>?token=`, share endpoint token-gated, QR SVG renders, `/join` serves the shell; PUBLIC: public config, demo+import blocked 403 without admin key, creation works with key and `share_url` uses `TD_MOBILE_PUBLIC_URL`, wrong admin key 403, crew opens job from share link, wrong token 404. `python scripts/mobile_app_check.py` PASS — user 20/20, TLS 11/11, share 19/19, stress clean (map-state p95 2.7ms<250ms). Error paths: missing/wrong admin key, wrong job token, missing-`segno` 503 (graceful), `cloudflared`-not-found launcher message all exercised. Lint + `py_compile` clean. Audit: `logs/mobile_check/latest.md`.
- note: `cloudflared` is an external field dependency (not bundled) — the launcher prints install instructions if absent; the tunnel itself isn't part of the in-process proof (network/edge), the share-only access logic it relies on is fully proven.
- push: feature branch `feature/mobile-web-pwa` (mobile-only/additive). Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.

## [Cursor Log] Mobile phase 2 — link expiry/revoke + always-on host deploy

- failed: two share-mode gaps the Director asked to close. (1) A lost/forwarded phone could open a job forever — the share link was the only credential with no kill switch and no timeout. (2) The public URL only existed while the laptop ran the Cloudflare tunnel, so the link died when the laptop closed; no always-on URL.
- changed (revoke/expiry): `mobile_web/store.py` — `create(expires_in_hours=…)` stamps `revoked=False` + `expires_at`; new `link_status()` (`active`/`revoked`/`expired`, tolerant of legacy jobs), `revoke()` and `set_expiry()` (also un-revokes/extends); `get_authorized()` now refuses revoked/expired even with a valid token; `public_job()` surfaces `link_status`/`expires_at`. `mobile_web/settings.py` — `default_link_ttl_hours()` (`TD_MOBILE_LINK_TTL_HOURS`). `mobile_web/server.py` — admin-gated `POST /api/jobs/{id}/revoke`, `POST /api/jobs/{id}/extend`, `GET /api/jobs/{id}/status`; per-job TTL on `demo`/`import` (`expires_in_hours` form/query → server default). `scripts/manage_share_link.py` (new) — laptop CLI to revoke/extend/status against any live host via admin key (accepts a bare id or a full `/join` link).
- changed (deploy): repo-root `Dockerfile` (python:3.12-slim, server-only deps, `uvicorn … --proxy-headers` so share links come back `https` behind the platform edge), `.dockerignore` (drops `.venv`/`logs`/`tds_data`/`cache`/`ui`/`packaging` → build context 326MB→~13kB), `deploy/requirements-host.txt` (fastapi/uvicorn/multipart/segno/pandas/numpy/openpyxl/networkx — no Qt/osmnx), `render.yaml` (Blueprint: docker web svc, generated admin key, `TD_MOBILE_LINK_TTL_HOURS=16`, 1GB disk at `/app/tds_data`, healthcheck `/api/healthz`), `deploy/fly.toml` (alternative), `scripts/mobile_host_smoke.py` (verifies a live/containerized host over real HTTP, simulates the TLS edge to prove https links), `docs/MOBILE_HOST_DEPLOY.md` (Render + Fly walkthrough, verify/manage/limits). Refreshed `scripts/mobile_app_check.py` backlog + share strength text. Desktop path untouched.
- rationale: the job token already scoped access; revoke/expiry add a kill switch + auto-timeout for the shift without inventing multi-user auth. For 24/7 without the laptop, the same FastAPI app just needs an always-on box — Docker + a Blueprint keeps it a few clicks and lets the Director run it on Render or Fly. `--proxy-headers` derives `https` from the edge so links are correct without pinning a URL.
- result: `python scripts/mobile_share_prove.py` PASS 32/32 (adds: local revoke; admin-only revoke 403 w/o key; revoked token 404; admin status=revoked; extend re-activates; expiry simulated → token 404 + status=expired; extend after expiry re-opens). `python scripts/mobile_app_check.py` PASS — user 20/20, TLS 11/11, share 32/32, stress clean. Deploy proven for real: `docker build` OK; ran the container and `python scripts/mobile_host_smoke.py` PASS 10/10 (healthz, public mode, creation 403 w/o key + 200 with key, https share link via proxy-headers, crew open, revoke→404, extend→200) plus `/join` serves the PWA shell; verified image contains `core`/`road_router`/`scripts`/`web/vendor`/`demo_data` and excludes `ui`. Error paths: wrong/missing admin key, revoked + expired tokens, unreachable host + 403 in the CLI. Lint clean on all changed files.
- note: Render/Fly accounts + actual cloud deploy are Director-side (external signup); everything was proven locally against the exact deploy image. Hosted mode means job data lives on the host — documented; same-Wi-Fi mode remains for data that must not leave the machine.
- push: feature branch `feature/mobile-web-pwa` (mobile-only/additive). Push held — same Director-review gate as the rest of this branch (base `main`).

## [Cursor Log] Mobile manual stop reorder (choose your own order, like the laptop)

- failed: the phone PWA could only auto-optimize the route ("Build route") — no way to manually set stop order like the desktop's "Move stop up/down" + "Re-trace route only". Director wants the mobile lane to match the laptop's choose-your-order control, mobile-friendly and crash-proof. First implementation auto-re-traced on every move; stress flagged `rapid_reorder` p95=4768ms (90 moves → 364s total) because this box has the full CA road graph, so each tap ran a complete street trace — a field freeze risk.
- changed: `mobile_web/store.py` — `move_stop(job, uid, dir)` pure list reorder returning `moved`/`edge`/`not_found` (no persist; caller saves). `mobile_web/server.py` — `POST /api/jobs/{id}/stops/{uid}/move` (cheap reorder + renumber only, marks `route.stale`, top/bottom is a 200 no-op, bad dir 400, unknown stop 404) and `POST /api/jobs/{id}/retrace` (re-draw the drive line for the current order, no re-optimize) via a shared `_trace_current_order()` helper that swallows routing errors so a bad graph/leg can never crash the phone. `core/map_state.py` — surfaces `route.stale`. PWA: `app.js` adds a Reorder mode (per-stop ▲/▼ 44px touch targets, busy-guard against double taps, stale "order changed — re-trace" prompt, resets on build/close), `index.html` adds Reorder/Re-trace buttons + hint and bumps `app.js?v=4`, `style.css` adds reorder/active styles, `sw.js` shell cache → v3. Decision: moving only reorders (instant); the drive line redraws once on explicit Re-trace — mirrors the desktop and keeps field taps snappy. Desktop path untouched (additive).
- rationale: separating "set the order" (cheap) from "trace the line" (potentially seconds with a road graph) is the only way to keep rapid field taps responsive and avoid a perceived crash; it also matches the laptop's two-step Move/Re-trace UX the Director already knows.
- result: `python scripts/mobile_app_check.py` PASS — user 26/26 (adds reorder up/down, edge no-op, bad-dir 400, unknown-stop 404, re-trace, order-restore), TLS 11/11, share 32/32, stress all green incl. new `rapid_reorder` (90 moves, 0 errors, stops intact, **p95 6.1ms** down from 4768ms; full run 364s→14.5s), map-state p95 5.1ms<250ms, concurrent writes safe. Error paths exercised: top/bottom no-op, invalid direction, unknown uid, and routing failure during re-trace leaving the prior line intact. Lint clean on all changed files. Audit: `logs/mobile_check/latest.md`.
- push: feature branch `feature/mobile-web-pwa` (mobile-only/additive). Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.

## [Cursor Log] Mobile open-uploads mode — upload Excel + .EST from the phone on the public URL

- failed: Director opened the online (Cloudflare tunnel) link on a phone and saw demo Site 101–105 they never uploaded, and "couldn't clear anything." Diagnosis from live checks: (1) the tunnel was started `run_mobile_share.py --demo`, which seeds the demo job and hands out that job's `/join` link; (2) the public tunnel runs share-only (`TD_MOBILE_PUBLIC=1`), which by design hides the create/import UI and admin-gates creation — so a phone on the public URL could only open the demo share link, never upload its own Excel/.EST. Clear actually worked (verified in-browser) but re-opening the demo `/join` link reloaded it, so it felt stuck. Director's real need: the online link must let the phone upload its own job (no laptop/LAN, no desktop hand-off).
- changed: `mobile_web/settings.py` — new `open_uploads()` (`TD_MOBILE_OPEN_CREATE=1`); `creation_allowed()` returns True when it's on (lets a phone create/import on a public tunnel without the admin key). `mobile_web/server.py` `/api/config` now reports `can_create = creation_allowed(None)` (true under open-uploads even in public mode). PWA: `app.js` `applyPublicMode(public, can_create)` adds a `public-open` body class when public+creatable; `style.css` `body.public-open` re-shows `#createBlock` and hides the share-only note; `index.html` `app.js?v=6`; `sw.js` shell cache → v6 so phones fetch the new JS/CSS. Launcher: `scripts/run_mobile_share.py` gains `--open-uploads` (sets the env, no `--demo`) and prints an OPEN-uploads banner. Desktop + share-only path untouched.
- rationale: keep the locked-down share-only default for crew-link deploys, but add one explicit operator opt-in for the single-operator field case where the same person who runs the tunnel also uploads the job from their phone. The tunnel URL is random/unguessable and short-lived, so open creation on it is acceptable for that use and stays off by default.
- result: PROVEN both sides. Coder: against a local `TD_MOBILE_PUBLIC=1 TD_MOBILE_OPEN_CREATE=1` server, `/api/config` → `public_mode:true, can_create:true`; `POST /api/jobs/demo` → 200 with NO admin key. Regression: the still-running share-only server (port 8800) returns 403 on the same call, so the default lockdown is intact. User: loaded the page in-browser (localhost and the live `https://learned-labour-gods-carb.trycloudflare.com` tunnel) — the start screen shows Load demo + Start lat,lon + Excel/CSV (Choose Files) + .EST (Choose Files) + Import job; a bare-URL reload stays on the start screen and does NOT auto-open an old job (cache/session check, sw bumped to v6). Restarted the live tunnel with `--open-uploads` (no demo); `public_url.txt` updated to the new URL. Lint clean on all changed files.
- note: trycloudflare quick-tunnel URLs are ephemeral — the live URL changes each launch; the active one is in `logs/mobile_check/public_url.txt`. Open-uploads is opt-in (`--open-uploads`); without it the public tunnel stays share-only.
- push: feature branch `feature/mobile-web-pwa` (mobile-only/additive). Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.
