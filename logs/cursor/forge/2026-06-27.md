# Cursor Log — 2026-06-27

## [Cursor Log] Mobile Web PWA (phone field runner)

- failed: no mobile/web lane existed — desktop-only (PySide6 + Qt WebEngine + USB GPS + PicoCount). First stress run also flagged `rapid_map_state` p95=181ms over a 150ms budget, and an optional 80-stop street-trace took 227s on this box (full CA road graph present).
- changed: added a standalone online-first lane that reuses desktop core unchanged. New: `core/map_state.py` (Qt-free map payload); `mobile_web/` (`server.py` FastAPI, `store.py` token-scoped JSON job store with in-memory cache, `static/` lean 4-tab PWA — Route/Install/Pickup/Audit — with phone-geolocation grab + drop-pin fallback, service worker, manifest, vendored MapLibre + online OSM tiles); `scripts/run_mobile.py` + `RUN_MOBILE.bat` (LAN launcher); proof scripts `scripts/mobile_user_prove.py`, `scripts/mobile_stress_loop.py`, `scripts/mobile_app_check.py`; `mobile_web/requirements.txt`; `docs/MOBILE_WEB.md`. Improvements from the stress findings: added the store in-memory cache (O(1) loads), and reworked the large-job scenario to measure the payload/edit latency the phone actually hits instead of the heavy server-side street-trace.
- rationale: Director wants a lean, fast, smooth phone version on phone internet — not a desktop clone. USB GPS and PicoCount are laptop-only by design; the phone captures exact location from the browser at install. Desktop app left untouched (new files only; `core/map_state.py` is additive and unused by the desktop path).
- result: `python scripts/mobile_app_check.py` PASS — user flow 20/20 (import, route, phone GPS grab, pin fallback, install, pickup, export, error paths; token enforced; out-of-bounds GPS + bad upload rejected); stress PASS (install cycle clean over 5 stops; map-state p50=110ms/p95=166ms under 250ms budget; concurrent grab+patch no lost writes; 80-stop map-state 36.7KB). Audit: `logs/mobile_check/latest.md`. Lint clean on all new files.
- regression: `python scripts/smoke_full.py` — functional checks PASS (core imports, road graph 11544 nodes, field readiness 100/100, offline gates, golden demo route in band). 2 FAILs are route-build **time budgets only** on a slow/loaded box (`benchmark_route` >300s; golden `n20`/`n40` 147s/241s vs 60s/86s), with correct mileage (2.6 mi, in band), in routing code this change never touches — pre-existing environmental, not a mobile-lane regression. Escalating the perf budgets to Director rather than tuning unrelated routing.
- push: committed to feature branch `feature/mobile-web-pwa`; **push held** (base is `main`, and desktop smoke not 100% green due to unrelated perf budgets) — Director to review/promote.

## [Cursor Log] Mobile HTTPS — phone GPS now works over LAN

- failed: the mobile lane's #1 field blocker — phone browser geolocation needs a secure context, but the launcher served plain `http://<lan-ip>:8800`, so `getCurrentPosition` was silently blocked in the field; only the drop-pin fallback worked. The whole point of the phone lane (capture exact install GPS) was unusable off `localhost`.
- changed: added `mobile_web/tls.py` (self-signed cert generator; SAN names this PC's LAN IP + `127.0.0.1` + `localhost`; cached in git-ignored `tds_data/mobile_certs/`, regenerated on host/expiry change). Reworked `scripts/run_mobile.py` to default to HTTPS (passes `ssl_certfile`/`ssl_keyfile` to uvicorn), print `https://` URLs + a one-time-warning note, with `TD_MOBILE_HTTP=1` opt-out and graceful HTTP fallback if `cryptography` is missing. Added `cryptography` to `mobile_web/requirements.txt` and the `RUN_MOBILE.bat` dep check. New proof `scripts/mobile_tls_prove.py` (boots real uvicorn HTTPS, pins trust to our cert). Wired `tls_prove` into `scripts/mobile_app_check.py` and refreshed its improvement backlog; updated `docs/MOBILE_WEB.md`. Desktop path untouched (additive; `core`/Qt shell unchanged).
- rationale: a self-contained field tool can't depend on an external HTTPS proxy. A self-signed cert that names the LAN IP gives a real secure context so the phone's "Grab GPS" works after a one-time "proceed" — no internet, no keys in the repo.
- result: `python scripts/mobile_tls_prove.py` PASS 11/11 (cert SAN has LAN IP + loopback + localhost; cert reused when fresh; real TLS handshake serves `/api/healthz`=200 ok; plain HTTP rejected on the TLS port; `TD_MOBILE_HTTP=1` opt-out resolves to no cert). `python scripts/mobile_app_check.py` PASS — user 20/20, TLS 11/11, stress clean (map-state p95 2.6ms<250ms, install cycle clean, concurrent writes safe). Error path: plain-HTTP-on-TLS-port and missing-`cryptography` fallback both exercised. Lint + `py_compile` clean on all four files. Audit: `logs/mobile_check/latest.md`.
- push: feature branch `feature/mobile-web-pwa`; mobile-only/additive. Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.

## [Cursor Log] Mobile public SHARE mode (text a link, phone works anywhere)

- failed: the mobile lane only worked on the same Wi-Fi/hotspot — no way to send a crew member a link that opens a job from cellular. Director wanted the old Streamlit "share a link, phone works anywhere" behavior. Recommendation taken: keep the FastAPI/PWA (reuses desktop `core/`), add share-only public mode + a tunnel, rather than re-adopting Streamlit (removed at v1.0.2 for the offline desktop tool).
- changed: `mobile_web/settings.py` (new) — share-only public mode (`TD_MOBILE_PUBLIC=1`), admin key (`TD_MOBILE_ADMIN_KEY`, auto-generated if unset in public mode), and public base URL (`TD_MOBILE_PUBLIC_URL`). `mobile_web/server.py`: `/join/{id}` share-link entry (serves the PWA, client auto-opens the job + strips the token from the URL), `/api/jobs/{id}/share` (link) and `/api/jobs/{id}/share.svg` (QR via `segno`), `share_url` in create responses, admin-gated `demo`/`import` creation, and `public_mode`/`can_create` in `/api/config`. PWA: `app.js` parses `/join/<id>?token=`, applies public mode (hides creation, shows share note), Audit-tab share link + Copy + QR; `index.html` wraps creation in `#createBlock`, adds share UI, bumps `app.js?v=2`; `style.css` public-mode + share styles. Launcher: `scripts/run_mobile_share.py` + `RUN_MOBILE_SHARE.bat` — public mode + `cloudflared` quick tunnel, discovers the public URL, feeds it back for absolute links, `--demo` prints a ready-to-send share link. Proof `scripts/mobile_share_prove.py`; wired `share_prove` into `mobile_app_check.py` + refreshed backlog; `segno` added to `mobile_web/requirements.txt` and both RUN_MOBILE bats; `docs/MOBILE_WEB.md` updated. Desktop untouched.
- rationale: each job already had a random token, so the access model was there — it just needed a public URL, a `/join` share link, and a way to stop random visitors creating/browsing jobs. Cloudflare quick tunnel gives a public HTTPS URL with no port-forwarding; the link itself is the per-job credential and is removed from the address bar after load. Always-on hosting (Render/Fly/VPS) is deliberately deferred to phase 2.
- result: `python scripts/mobile_share_prove.py` PASS 19/19 — LOCAL: not-public config, demo without key, absolute `share_url` with `/join/<id>?token=`, share endpoint token-gated, QR SVG renders, `/join` serves the shell; PUBLIC: public config, demo+import blocked 403 without admin key, creation works with key and `share_url` uses `TD_MOBILE_PUBLIC_URL`, wrong admin key 403, crew opens job from share link, wrong token 404. `python scripts/mobile_app_check.py` PASS — user 20/20, TLS 11/11, share 19/19, stress clean (map-state p95 2.7ms<250ms). Error paths: missing/wrong admin key, wrong job token, missing-`segno` 503 (graceful), `cloudflared`-not-found launcher message all exercised. Lint + `py_compile` clean. Audit: `logs/mobile_check/latest.md`.
- note: `cloudflared` is an external field dependency (not bundled) — the launcher prints install instructions if absent; the tunnel itself isn't part of the in-process proof (network/edge), the share-only access logic it relies on is fully proven.
- push: feature branch `feature/mobile-web-pwa` (mobile-only/additive). Push of the feature branch per forge-autonomy (non-`main`); merge to `main` still Director-gated.
