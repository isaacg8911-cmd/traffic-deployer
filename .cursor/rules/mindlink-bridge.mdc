---
description: Traffic Deployer workspace — app scope; OS rules live in MindLink AI root
alwaysApply: true
---

# MindLink bridge (this app)

This folder is **Traffic Deployer only**. Canonical OS contract: `C:\MindLink AI\.cursor\rules\`.

**Do not** edit MindLink OS rules, `mindlink/objective.md`, or platform scripts unless Director explicitly requests an OS change.

**Do** read this repo's `SCOPE.md`, `SCOPE_PACKAGES.md`, and `project.mdc`.

**Director does not run** smoke or setup — Forge runs `SMOKE.bat` / `scripts/smoke_full.py` and reports exit code.

**Better workspace:** open `C:\MindLink AI\mindlink-os.code-workspace` to load OS + app rules together.
