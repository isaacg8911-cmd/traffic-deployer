## [Cursor Log] DEMAND: two products only (Render + work-laptop exe)

- **failed:** Multiple parallel “versions” — Fly.io, local LAN mobile (`RUN_MOBILE.bat`), Cloudflare tunnel (`RUN_MOBILE_SHARE.bat`), standalone portable build (`BUILD_PORTABLE.bat`), one-off extract scripts, stale docs.
- **changed:** Locked **SCOPE.md** + **project.mdc** to two surfaces only. Deleted Fly deploy, local mobile launchers, `mobile_web/tls.py`, extract_* scripts, historical meta docs, `PORTABLE_INSTALL.txt`. Renamed `build_portable.ps1` → `build_exe.ps1`, `verify_portable.py` → `verify_exe_build.py`. Updated handover/docs/README. Trimmed `mobile_app_check` (no tls_prove). Browser prove skips cleanly when Playwright chromium missing.
- **rationale:** Director DEMAND — stop maintaining alternate delivery paths; internal PyInstaller step stays as build input for work-laptop zip only.
- **result:** `smoke_full.py` exit 0; `mobile_app_check.py` exit 0 (browser_prove skipped — no chromium binary).

## [Cursor Log] Prove both products actually run + fix Render boot dep

- **failed:** New lean `Dockerfile` does `COPY web/vendor/`, but `web/vendor/` is fully gitignored — Render builds from GitHub (not local disk), so MapLibre would be missing and the PWA would never boot (share links dead). Local docker build hid this because it reads the filesystem.
- **changed:** `.gitignore` now ignores `web/vendor/*` but force-tracks `web/vendor/maplibre-gl.js` + `maplibre-gl.css` (the only vendor files the mobile raster PWA needs; pmtiles/qwebchannel/fonts stay ignored). Both files added to repo.
- **rationale:** Make the Render image self-contained and deterministic without build-time network; guarantee the GitHub→Render build has the PWA boot dependency.
- **result (Render):** `docker build` OK; container ran with Render env (`PORT`/`TD_MOBILE_PUBLIC`/`TD_MOBILE_ADMIN_KEY`). Proved over real HTTP: `/api/healthz` ok+public; `/vendor/maplibre-gl.js` 200 (803 KB) + `.css` 200; create without admin key → 403; create with key → job (5 stops) + share_url; open via token ok; build route ok (graph=False straight-line, miles computed); wrong token → 404; `/join/<id>` → 200.
- **result (work laptop):** `scripts/build_exe.ps1` built `dist/TrafficDeployer` (14.8 MB exe); `scripts/verify_exe_build.py` → **EXE BUILD VERIFY PASS** (frozen web assets + map HTTP + demo_workflow source & frozen).

## [Cursor Log] DEMAND: equal-use self-serve mobile (Render Option A)

- **failed:** Hosted Render PWA was share-only — only the admin key could create jobs, and revoke/extend/status were admin-key gated. Coworkers could not bring their own files or manage their own job without the Director; not an equal field-work app.
- **changed:**
  - `render.yaml`: added `TD_MOBILE_OPEN_CREATE=1` (public start screen lets each coworker import their own Excel/.EST → separate tokenized job). Stays `TD_MOBILE_PUBLIC=1` + generated admin key.
  - `mobile_web/server.py`: split creation gate (`_require_create_allowed`, honors open-create) from a new `_authorize_manage` so revoke/extend/status are authorized by the **job's own link token** (equal self-service) with the admin key as optional fallback; removed dead `_require_admin`; added `import secrets`.
  - `mobile_web/settings.py`: added `admin_allowed()` so open-uploads never makes link management public on Render; refreshed docstrings (self-serve vs share-only).
  - Proof scripts: `mobile_share_prove.py` now proves open-create isolation + equal self-management (own-token status/extend/revoke/un-revoke, cross-job blocked, anon blocked, admin fallback). `mobile_host_smoke.py` + docs commands take `--open-create`; live smoke proves self-revoke-by-token + admin fallback. `mobile_app_check.py` audit text updated.
  - Docs: `docs/MOBILE_HOST_DEPLOY.md`, `docs/MOBILE_WEB.md` rewritten for self-serve coworker import + token self-management.
- **rationale:** Director DEMAND — field app, everyone equal; no operator tier. Each coworker uses their own files in an isolated job and controls their own link; admin key kept only as a lost-phone fallback. Token scoping prevents one coworker from touching another's job.
- **result:** `scripts/mobile_share_prove.py` → 50/50. `scripts/mobile_app_check.py` → **PASS** (user 29/29, share 50/50, stress clean: map-state p95 216.8ms/250ms, install 5/5, concurrent writes 0 lost; browser_prove skipped — no chromium). Lints clean. Not yet redeployed to Render — push triggers GitHub→Render build; verify live with `mobile_host_smoke.py --open-create`.

## [Cursor Log] DEMAND: mobile map dots — readable site #, all dots visible, GPS after INSTALL

- **failed:** Install tab hid all site dots except the current stop; site popup used dark panel styling (hard to read on map tiles); grabbed/dropped GPS saved to server but never appeared on the map after INSTALL.
- **changed:** `mobile_web/static/app.js` — show all color-coded begin/end dots on every map tab; new `field-gps` layer renders a larger dot at `field_lat`/`field_lon` once `installed`; tap any dot (reference or GPS) opens a high-contrast popup with prominent site number. `style.css` — white popup card, 28px site id, dark badge. Cache bust `app.js?v=16`.
- **rationale:** Director DEMAND — field crew must see every site on the map, read site numbers on tap, and keep installed GPS locations visible permanently.
- **result:** `scripts/mobile_app_check.py` exit 0 (user 29/29, share 50/50, stress clean, browser 12/12).
