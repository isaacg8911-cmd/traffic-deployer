## [Cursor Log] DEMAND: two products only (Render + work-laptop exe)

- **failed:** Multiple parallel “versions” — Fly.io, local LAN mobile (`RUN_MOBILE.bat`), Cloudflare tunnel (`RUN_MOBILE_SHARE.bat`), standalone portable build (`BUILD_PORTABLE.bat`), one-off extract scripts, stale docs.
- **changed:** Locked **SCOPE.md** + **project.mdc** to two surfaces only. Deleted Fly deploy, local mobile launchers, `mobile_web/tls.py`, extract_* scripts, historical meta docs, `PORTABLE_INSTALL.txt`. Renamed `build_portable.ps1` → `build_exe.ps1`, `verify_portable.py` → `verify_exe_build.py`. Updated handover/docs/README. Trimmed `mobile_app_check` (no tls_prove). Browser prove skips cleanly when Playwright chromium missing.
- **rationale:** Director DEMAND — stop maintaining alternate delivery paths; internal PyInstaller step stays as build input for work-laptop zip only.
- **result:** `smoke_full.py` exit 0; `mobile_app_check.py` exit 0 (browser_prove skipped — no chromium binary).

## [Cursor Log] Prove both products actually run + fix Render boot dep

- **failed:** New lean `Dockerfile` does `COPY web/vendor/`, but `web/vendor/` is fully gitignored — Render builds from GitHub (not local disk), so MapLibre would be missing and the PWA would never boot (share links dead). Local docker build hid this because it reads the filesystem.
- **changed:** `.gitignore` now ignores `web/vendor/*` but force-tracks `web/vendor/maplibre-gl.js` + `maplibre-gl.css` (the only vendor files the mobile raster PWA needs; pmtiles/qwebchannel/fonts stay ignored). Both files added to repo.
- **rationale:** Make the Render image self-contained and deterministic without build-time network; guarantee the GitHub→Render build has the PWA boot dependency.
- **result (Render):** `docker build` OK; container ran with Render env (`PORT`/`TD_MOBILE_PUBLIC`/`TD_MOBILE_ADMIN_KEY`). Proved over real HTTP: `/api/healthz` ok+public; `/vendor/maplibre-gl.js` 200 (803 KB) + `.css` 200; create without admin key → 403; create with key → job (5 stops) + share_url; open via token ok; build route ok (graph=False straight-line, miles computed); wrong token → 404; `/join/<id>` → 200.
- **result (work laptop):** `scripts/build_exe.ps1` built `dist/TrafficDeployer` (14.8 MB exe); `scripts/verify_exe_build.py` → **EXE BUILD VERIFY PASS** (frozen web assets + map HTTP + demo_workflow source & frozen).
