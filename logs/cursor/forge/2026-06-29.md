[Cursor Log] 2026-06-29 — Mobile: user-resizable map/menu split (drag handle)

**ask:** Let the field user slide the Route/Install/Pickup menu up or down to see more or less of the map. Previously the split was locked at panel max-height 55%.

**changed:**
- `mobile_web/static/index.html` — added `#sheetHandle` grab-bar between `#mapWrap` and the panels; bumped `app.js?v=16` → `v17`.
- `mobile_web/static/style.css` — new `--sheet-pct` var (default 50) drives panel height (`min 92px`, `max 82%`); `.sheet-handle` styling (26px ns-resize bar with grip, focus/drag highlight).
- `mobile_web/static/app.js` — `initSheet()` drag logic via pointer events (pointerdown/move/up + capture), `setSheetPct()` clamped 24–80%, persists to `localStorage['td_sheet_pct']`, ArrowUp/Down keyboard nudge, `map.resize()` on rAF + drag end; handle toggled with map in `setTab`/`showStart`.
- `mobile_web/static/sw.js` — shell cache `v9` → `v10` so the cache-first `style.css` change reaches phones.

**proof (coder):** `scripts/mobile_app_check.py` — user_prove 29/29, share_prove 50/50. Pre-existing FAILs unrelated to this change: stress_loop (server latency budget: rapid_map_state p95 532ms>250ms, 1 concurrent-write err) and browser_prove (`local server did not start` — in-proc uvicorn didn't bind in 30s after heavy stress step; never loaded UI).

**proof (user, real Chromium against local uvicorn):**
- Loaded share link → app shell + map render; grip bar visible between map and panel.
- Drag up: pct 50→65.9, panel 401→528px, map 271→144px; persisted to localStorage.
- Drag down: pct→31.8, map→417px, panel→255px.
- Drag far down clamps at min 24%.
- Full page reload (saved-session): split restored to 24% (map 479 / panel 192) — not reset to default.
- Handle hidden on Audit tab, reappears on Install, split preserved.

**result:** Map/menu split is user-adjustable and remembered per-phone. Map renders correctly at the new sizes (resize fires). Not a HANDOFF-PROOF claim.

**git:** branch `feature/mobile-web-pwa` (not main/master) — auto-commit + push after proof.

---

[Cursor Log] 2026-06-29 — Mobile: compact + touchable UI polish (neat/minimal)

**ask:** Make the mobile UI compact and touchable at the same time — neat, professional, minimal.

**approach:** Decouple density from tap size. Added geometry tokens (`--r` 10px, `--r-lg` 14px, `--hit` 44px) and used `min-height: var(--hit)` on every control so spacing/padding could tighten without any target dropping below 44px.

**changed (`mobile_web/static/style.css`):**
- Buttons/inputs/selects: padding-driven height → `min-height: var(--hit)` (44px); main actions (INSTALL/SKIP/big) 52px. One radius token across cards/buttons/inputs/lists. Added uniform press feedback (`:active` translateY) + input focus ring (blue border).
- Field labels: smaller, uppercase, letter-spaced — quieter, more professional.
- Lists: gap 6→5, padding 11→7px with `min-height: 44px`; smaller dots; `sub` line truncates with ellipsis; current row = thin inset outline instead of heavy 2px.
- Panel gap 10→8; start card/divider tightened; tab bar labels lighter/uppercase-spaced with 2px active rail; topbar/tabbar 48/56 → 46/54.
- `sw.js`: shell cache `v10` → `v11` so the cache-first CSS reaches phones.

**proof (coder):** no lint errors (style.css, sw.js).

**proof (user, real Chromium, SW unregistered to force fresh CSS):**
- New CSS live: `--r`=10px, `--hit`=44px, field labels computed `text-transform: uppercase`.
- Touch targets measured: Build route 44, Reorder 44, list item 53, tab btn 53, topbar 46; Install tab — Street input 44, Direction select 44, Prev 44, INSTALL/SKIP 52. All ≥44px.
- Visual check Route + Install tabs: compact, consistent radii, calmer hierarchy.

**result:** Denser layout, every control still finger-sized. Pure CSS + cache bump; no behavior/JS change. Not a HANDOFF-PROOF claim.

**git:** branch `feature/mobile-web-pwa` — auto-commit + push after proof.
