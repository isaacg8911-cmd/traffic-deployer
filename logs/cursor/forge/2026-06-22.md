## [Cursor Log] App update zip for work laptop (v1.0.9)

- Extended `pack_app_update.py` to emit `dist/TrafficDeployer-AppUpdate.zip`.
- Ran `build_app_update.ps1` — APP UPDATE GATE PASS; zip 299 MB (789 MB unpacked).
- Updated `packaging/APP_UPDATE.txt` with zip install steps.

## [Cursor Log] DEMAND: smoke_full --gps-only (ASC Tier 2)

- Added `--gps-only` flag to `scripts/smoke_full.py` (was missing; full suite had been ignoring the arg).
- Attempt 1: fixed two bad assertions in new `test_gps_only()` (app.js `pushGps`/`renderGps`; `field_coords_from_stops` needs `installed`).
- Proof: `python scripts/smoke_full.py --gps-only` → exit 0, ~6s, all checks green.

## [Cursor Log] DEMAND: manual grab map → GPS → UI pipeline (ASC Tier 2)

- Exercised START.bat stack: `quick_preflight.py` pass + `prove_manual_grab_15228.py` (MainWindow / WebEngine, same as `python main.py`).
- Fixed silent pin-drop proof: WebChannel reentrancy swallows JS return when `fireMapClick` runs inside `runJavaScript`; split pin (`__tdShowDropPinOnly` + `__tdLastPinDrop`) from handler (`bridge.onMapClick` → `_manual_grab_at`).
- Added `web/app.js`: `__tdLastPinDrop`, `__tdShowDropPinOnly`, `__tdSimManualGrabClick`; hardened `scripts/test_tdmap_webengine.py` QApplication init.
- Proof: `prove_manual_grab_15228.py` exit 0 — pin drop, sidebar `lbl_grab`, field_lat/lon saved; artifact `logs/manual_grab_proof/15228_proof.json`.

## [Cursor Log] DEMAND: tune app for work laptop (N200 / 4 GB)

- Target: `LAPTOP-FLHPOPIS` — Intel N200, 4 GB RAM, ~24 GB free disk (from Director `worklaptop.txt`).
- Added `core/hardware_profile.py`: auto-detect RAM ≤5.5 GB or `TDS_WORK_LAPTOP=1`; tighter Chromium cache/JS heap; smaller default window.
- `ui/simple_mode.py`: work-laptop timing tier (between full and battery saver); lean map in field mode on low RAM.
- `main.py`: profile-aware WebEngine cache, status bar **Work laptop**, non-maximized launch on low RAM.
- `packaging/OPEN_APP.bat`: sets `TDS_WORK_LAPTOP=1`; `WORK_LAPTOP.md` + `HOW_TO_RUN.md` updated.
- Proof: `python scripts/smoke_full.py` → SMOKE PASS.

## [Cursor Log] DEMAND: Volume by Lane CSV (1029 format)

- Added `core/picocount_hits.py` + `core/volume_report.py` — parse `.pcbin`/`.tvp`, pair A/B hose hits, emit TrafficViewer-style CSV.
- Pickup: **Export volume CSV (1029 format)** · Audit: **Export all volume CSVs**.
- CLI: `python scripts/volume_report.py study.pcbin` · output `tds_data/exports/volume/<unit>_volume.csv`.
- Proof: `15126ec1b_live.pcbin` → 295 vehicles, East/West columns; smoke volume report checks green.

- `core/export.py`: `parse_ig_tfc_workbook` + `apply_ig_tfc_progress` — IG TFC Installed column is truth; strip field GPS from non-installed stops.
- `scripts/open_week14_for_edit.py`: loads `week 14 ig tfc.xlsx` + `TDS_Report_DEFAULT_2026-06-17 (1).xlsx`; focus site 15228 (missing GPS for manual grab).
- `web/app.js`: green install dots only when `installed && field_lat/lon`.
- Proof: `open_week14_for_edit.py --no-launch` → 40 installed, 39 GPS dots, 0 leaked non-installed GPS; missing: 15228.

## [Cursor Log] Route HTML export visible + v1.0.10 app update

- `ui/pages/route_page.py`: unhide Route HTML section in SIMPLE_MODE — **Save HTML route (site order)** / **Save HTML pickup order**.
- `version.py` → **1.0.10**; ran `build_app_update.ps1` — APP UPDATE GATE PASS.
- Output: `dist\TrafficDeployer-AppUpdate.zip` (299 MB) + folder `dist\TrafficDeployer-AppUpdate\TrafficDeployer\` (789 MB).
