[Cursor Log] 2026-06-24 — Drop pin lag + corner pin fix

**failed:** Manual grab froze map and showed orange pin in corner before map click. Prior proof only simulated bridge click after forcing pin — did not test "press Drop pin then interact with map".

**changed:**
- `main.py` — drop pin enter no longer calls full `_push_state()` (was triggering heavy route leg rebuild); manual pin saves use lightweight `send_field_pin` + debounced persist; street geocode moved to `FieldStreetThread`
- `bridge.py` / `web/app.js` — `set_manual_grab(prompt)`, `__tdPatchFieldPin`, clear stale pin on enter, validate CA coords, setLngLat before marker addTo, debounced drag save
- `scripts/prove_drop_pin_enter.py` — real UI proof: enter drop pin, assert no pin before click, map zoom still works

**rationale:** Corner pin was MapLibre marker default at 0,0 when created before valid coords; freeze was main-thread geocode + full map redraw on every pin event.

**result:**
- `prove_drop_pin_enter.py` OK
- `prove_manual_grab_15228.py` OK
- `smoke_full.py --gps-only` OK

---

[Cursor Log] 2026-06-24 — Universal user-input app check

**failed:** Initial `prove_user_paths` run failed on `test_tdmap_webengine.py` JS navigation tests — those assert `window.location.href` tdmap interception, which WebEngine does not support (production uses `bridge.onMapClick`).

**changed:**
- `scripts/app_check.py` — universal tiers (`smoke` / `headless` / `user` / `full`) + audit report (`logs/app_check/latest.md`)
- `USER_PROVE.bat` / `APP_CHECK.bat` — one-click user + full checks
- `prove_user_paths.py` — delegates to `app_check --tier user`
- `pre_ship_gate.py` — adds user-path proofs before ship
- `test_tdmap_webengine.py` — JS nav empty hits documented as expected

---

[Cursor Log] 2026-06-24 — Work laptop app update pack v1.0.10

**changed:** Ran `BUILD_APP_UPDATE.bat` pipeline — portable exe rebuild + `TrafficDeployer-AppUpdate` folder + zip.

**result:**
- `dist\TrafficDeployer-AppUpdate\TrafficDeployer` — 789 MB (exe + _internal + web)
- `dist\TrafficDeployer-AppUpdate.zip` — 299 MB
- App update gate PASS (string graph, field sim, portable demo, frozen bundle)

**changed:**
- `web/app.js` — MapLibre popup on blue begin / red end click while on Install tab (site #, street, point label)
- `main.py` — `on_install` map flag; begin/end clicks on Install no longer switch active stop (reference only)
- `web/index.html` — popup styles; app.js v27

**result:** `test_ui_wiring.py` OK (52/52)

**rationale:** Director demanded full user-input proof to surface flaws; headless smoke alone missed drop-pin enter regression. Audit report makes strengths/weaknesses/gaps repeatable for every app check.

**result:**
- `app_check.py --tier user` PASS (81s) — drop pin, manual grab 15228, install no block, desk drop-pin, map bridge, ui wiring, demo, field sim
- Audit weakness: stale marker visible before drop-pin click (cosmetic)
- Coverage gaps logged in `logs/app_check/latest.md`
