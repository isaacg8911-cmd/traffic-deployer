"""Traffic Deployer core logic (UI-independent).

These modules hold all the field-ops logic with NO Streamlit / Qt imports, so the
same brain can be driven by the desktop UI, a script, or tests.
"""
