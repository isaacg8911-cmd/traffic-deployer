"""Mobile web lane for Traffic Deployer.

A lean, online-first FastAPI + PWA companion that reuses the desktop core
(ingest, routing, export) but drops USB GPS, PicoCount, and the Qt shell.
See docs/MOBILE_WEB.md.
"""
